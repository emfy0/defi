<div class="max-w-6xl mx-auto mt-10">

  <div class="flex items-center justify-between mb-8">
    <h1 class="text-3xl font-semibold">Предложения</h1>

    <%= link_to "Создать новое предложение",
      new_offer_path,
      class: "bg-blue-600 hover:bg-blue-700 text-white mx-4 px-4 py-2 rounded-xl font-medium" %>
  </div>

  <% if @offers.any? %>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">

      <% @offers.each do |offer| %>
        <div class="bg-white p-6 rounded-2xl shadow border border-gray-100">

          <h2 class="text-xl font-semibold mb-3">
            <%= offer.payment_method.presence %>
          </h2>

          <div class="space-y-1 text-gray-700">
            <p><span class="font-semibold">Сумма:</span> <%= offer.value %></p>
            <p><span class="font-semibold">LTV %:</span> <%= offer.ltv_percent %></p>
            <p><span class="font-semibold">Процент:</span> <%= offer.interest_percent %></p>
          </div>

          <div class="mt-6 flex justify-between items-center">

            <%= link_to "Создать контракт",
                  new_offer_contract_path(offer),
                  class: "text-blue-600 hover:underline font-medium" %>
          </div>
        </div>
      <% end %>

    </div>
  <% else %>
    <p class="text-gray-600 text-lg mt-6">Предложения еще не были созданы.</p>
  <% end %>

</div>
